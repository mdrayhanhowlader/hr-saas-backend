[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "npx tsc --skipLibCheck"]

[start]
cmd = "npx prisma migrate deploy && node dist/index.js"
